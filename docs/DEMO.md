# Demo

Place a screenshot or an [asciinema](https://asciinema.org/) recording of a
`transcribe → translate → speak` run here, and embed it from the top of
`README.md`.

Quick text-mode capture (no microphone needed):

```
python -m picos_ce --from en --text "restraining order"
python -m picos_ce --from es --text "orden de restricción"
```
